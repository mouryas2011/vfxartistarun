# Packages installed inside the NEXORA development rootfs (Phase 1).
# Names are Debian package names.
linux-image-amd64
initramfs-tools
live-boot
systemd-sysv
udev
kmod
procps
ifupdown
isc-dhcp-client
openssh-server
# Installer runtime tools (Phase 2): partition, format, unpack, bootloader.
parted
dosfstools
squashfs-tools
grub-efi-amd64
# Graphical foundation (Phase 2): Wayland compositor + tools.
weston
wayland-utils
libwayland-dev
# Compile the NEXORA Wayland pixel client during the image build.
gcc
libc6-dev